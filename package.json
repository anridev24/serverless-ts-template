{
  "dependencies": {
    "dotenv": "^14.2.0",
    "serverless": "^2.72.2",
    "signale": "^1.4.0",
    "ts-node": "^10.8.0"
  },
  "devDependencies": {
    "@types/dotenv": "^8.2.0",
    "@types/node": "^17.0.13",
    "@types/signale": "^1.4.4",
    "husky": "^7.0.4",
    "prettier": "^2.3.2",
    "rimraf": "^3.0.2",
    "serverless-dotenv-plugin": "^3.12.2",
    "serverless-offline": "^8.3.1",
    "serverless-plugin-common-excludes": "^4.0.0",
    "serverless-plugin-include-dependencies": "^5.0.0",
    "serverless-plugin-scripts": "^1.0.2",
    "typescript": "^4.5.5"
  },
  "scripts": {
    "dev": "rimraf dist && mkdir dist && tsc -w",
    "dev-nosls": "ts-node src/main.ts",
    "offline": "serverless offline --httpPort 4001",
    "format": "prettier --write .",
    "build": "rimraf dist && mkdir dist && tsc"
  }
}
