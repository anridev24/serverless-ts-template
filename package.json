{
  "dependencies": {
    "concurrently": "^7.0.0",
    "dotenv": "^14.2.0"
  },
  "devDependencies": {
    "prettier": "^2.3.2",
    "rimraf": "^3.0.2",
    "serverless-offline": "^8.3.1",
    "serverless-plugin-scripts": "^1.0.2",
    "typescript": "^4.5.4"
  },
  "scripts": {
    "dev": "rimraf dist && mkdir dist && concurrently \"tsc -w\" \"serverless offline --httpPort 4001 \"",
    "format": "prettier --write .",
    "build": "rimraf dist && mkdir dist && tsc"
  }
}
